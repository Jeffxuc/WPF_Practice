<Window x:Class="Pr21_Resources.SubItem.Window1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Pr21_Resources.SubItem"
        mc:Ignorable="d"
        Title="The Window1" Height="450" Width="800">
    <StackPanel>
        <TextBlock HorizontalAlignment="Center" Foreground="{StaticResource brushGradient}" Margin="{StaticResource controlMargin}">
            This is first textblock,thanks for you.
        </TextBlock>
        <TextBlock HorizontalAlignment="Center" Foreground="{StaticResource brushGradient}" Margin="{StaticResource controlMargin}">
            Make the app great.
        </TextBlock>
        <TextBlock HorizontalAlignment="Center" Foreground="{StaticResource brushGradient}" Margin="{StaticResource controlMargin}">
            hello this is just a test for the wpf,thanks.
        </TextBlock>
        <TextBlock HorizontalAlignment="Center" Foreground="{StaticResource brushGradient}" Margin="{StaticResource controlMargin}">
            it a linear gradient color changed way.
        </TextBlock>
    </StackPanel>
</Window>
